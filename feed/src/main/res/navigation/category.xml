<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/menu_categories"
        app:startDestination="@id/category_feed">

    <fragment
            android:id="@+id/category_feed"
            android:name="com.cognota.feed.category.ui.CategoriesFeedFragment"
            tools:layout="@layout/fragment_categories_feed">
        <action
                android:id="@+id/detailAction"
                app:destination="@id/detailFeedActivity" />
    </fragment>

    <activity
            android:id="@+id/detailFeedActivity"
            android:name="com.cognota.feed.detail.ui.DetailFeedActivity"
            tools:layout="@layout/activity_feed_detail">
        <argument
                android:name="feed"
                android:defaultValue="@null"
                app:argType="com.cognota.feed.commons.domain.FeedDTO"
                app:nullable="true" />
    </activity>

</navigation>